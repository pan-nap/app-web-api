# 收银系统 · 整体架构梳理

> 本文档用于项目整体架构参考，为后续修改 / 新增功能提供依据。
> 生成时间基于当前代码现状（与根目录 README.md 互为补充，README 偏向功能清单，本文偏向结构与扩展点）。

---

## 1. 仓库总览（pnpm monorepo）

```
收银/
├── package.json            # 根工作区：统一 dev 脚本入口
├── pnpm-workspace.yaml     # packages/* 为工作区成员
├── tsconfig.json           # 根 TS 配置（noEmit，仅供编辑器）
├── README.md               # 功能清单 / 技术栈 / 接口文档
├── 项目文件夹说明.yaml       # 各端说明
├── .agents/
│   ├── PROJECT_RULES.md    # 开发规范总纲（必须先读对应 skill）
│   └── skills/             # 7 个开发规范 skill（见 §7）
└── packages/
    ├── web/                # Vue 3 后台管理系统（hs-sf-web）
    ├── backend/            # Koa 3 后端 API（hs-sf-backend）
    ├── app/                # UniApp X 移动端（@cashier/app）
    ├── emr/                # 医疗文书富文本编辑器库（@cashier/emr，被 web 引用）
    ├── mqtt-server/        # 本地 MQTT 测试服务器（@cashier/mqtt-server）
    └── ai-plugin/          # VSCode AI 插件（ai-plugin，独立于其他端）
```

**根工作区脚本**（`pnpm ...` 在根目录执行）：

| 命令 | 作用 |
|---|---|
| `pnpm dev` | 所有子包并行跑 dev（`pnpm -r --parallel run dev`） |
| `pnpm dev:web` | 仅启动 web（`--filter hs-sf-web`） |
| `pnpm dev:backend` | 仅启动 backend |
| `pnpm dev:app-android` | 启动 app Android 端（HBuilderX 通道） |

> ⚠️ 注意：根 `dev` 会把 `ai-plugin`（esbuild watch）、`emr`（vite dev）也一并跑起来，若只想起 web+backend 请用 `pnpm dev:web` + `pnpm dev:backend`。

---

## 2. 包间依赖关系

```
┌────────────┐   workspace:*   ┌──────────┐   本地 uni_modules    ┌─────────┐
│ web (Vue3) │ ──────────────► │ emr      │                      │ app     │
└─────┬──────┘                 └──────────┘                      └────┬────┘
      │  hs-admin-ui (OSS tgz 私有包)                                  │
      │  /sf-web/* 请求（vite proxy → :3001）                          │ /sf-web/*（直达 :3001）
      ▼                                                               ▼
┌────────────┐    HTTP + JWT    ┌──────────┐   mysql2 连接池   ┌───────────┐
│ backend    │ ◄──────────────► │ web/app  │ ────────────────► │ MySQL     │
│ (Koa 3)    │                  └──────────┘                   │           │
└─────┬──────┘                                                 └───────────┘
      ▲
      │  mqtt.js 客户端连接（ws://host:8098/mqtt 或 mqtt://host:1883）
┌─────┴──────┐
│ mqtt-server│（aedes 本地测试 broker）
└────────────┘

┌────────────┐
│ ai-plugin  │（VSCode 扩展，完全独立：无 workspace 依赖，直连 DeepSeek API）
└────────────┘
```

---

## 3. 各包详解

### 3.1 `packages/web` — 后台管理系统

- **技术栈**：Vue 3.5 + `<script setup>` + TS、Pinia 3、Vue Router 5、Vite 8、hs-admin-ui（内含 Element Plus + VXE-Table）、Tailwind、Vitest。
- **入口**：`src/main.ts` → 注册 Pinia、`Utils.message/notification/messageBox`、`useHsAdminUi`、`Utils.popupPlugin`（弹窗插件）、`emrEditor`（来自 @cashier/emr）。
- **路由**：`src/router/route.ts` 静态路由 + `src/router/index.ts` 全局守卫（JWT 登录态，未登录跳 `/login`）。
  - `/login` → `views/login/index.vue`
  - `/admin`（layout）子路由：`student`、`order`、`employee`、`upload`、`dictionary`、`documents`
- **分层规范（重要）**：
  - **Store 层**（`src/stores/`）：所有接口调用集中在 Pinia store（`user/dictionary/student/order/employee/documents`），页面只做 UI 展示。接口统一走 `Utils.useRequest("/sf-web/xxx", params, method)`。
  - **页面层**（`src/views/admin/<模块>/`）：`index.vue`（列表页）+ `gridOptions.ts`（vxe-grid 列配置）+ `popup/`（新增/编辑弹窗，`popup.vue` + `index.ts` 入口）+ 可选 `detailPopup/`、`batchEditPopup/`。
  - **hook**：`src/hook/useGridTableMethods.ts` 标准化 vxe-table 列表获取/刷新。
- **列表页标准范式**：gridOptions 配置 `url` + 列定义 → `useGridTableMethods()` 拿表格方法 → 弹窗走 `popupPlugin`。
- **文档模块**：`views/admin/documents/` 列表 + `popup/add.vue` 内嵌 `EmrDesigner`（来自 @cashier/emr）。
- **测试**：Vitest，store 单测（`src/stores/__tests__/`）+ gridOptions 单测，mock `Utils.useRequest`。

### 3.2 `packages/backend` — 后端 API

- **技术栈**：Koa 3、@koa/router、@koa/bodyparser、@koa/multer、mysql2（连接池）、jsonwebtoken、bcryptjs、ESM（`"type": "module"`）+ 路径别名 `#xxx/*`。
- **入口**：`src/index.js` → CORS → bodyParser（`/upload` 除外，走 multer）→ 静态目录（`/uploads`）→ `registerRoutes` → 404/500 兜底。
- **分层架构**（每新增一个模块按此五层复制）：

```
src/
├── routes/          # 路由定义（RESTful，批量操作用 POST 因为 DELETE 无法带 body）
├── controllers/     # 控制器（每模块目录：index.js + queries.js + mutations.js + batchOps.js + actions.js）
├── services/        # 业务逻辑层
├── models/          # 数据访问层（SQL）
├── validators/      # 输入校验层
├── middleware/auth.js  # JWT 认证中间件
├── config/database.js  # mysql2 连接池
└── utils/           # response.js（统一响应）、jwt.js、bcrypt.js、staticIndex.js
```

- **统一响应格式**（`utils/response.js`）：
  - 成功：`{ code: 1, data, msg }`
  - 失败：`{ code: 0, data: null, msg }`
  - 分页：`{ code: 1, data, total, msg }`
  - 控制器统一用 `wrap(fn)` 包裹，异常自动转成 `code: 0` 响应。
- **认证**：除登录外所有接口挂 `authMiddleware`，从 `Authorization: Bearer <token>` 取 JWT，解码结果放 `ctx.state.user`。
- **环境配置**（`.env`）：`PORT=3001`、`DB_NAME=student_manage`、`JWT_SECRET`、`JWT_EXPIRES_IN=7d`。
- **数据库表**：`user`、`students`、`orders`、`order_items`、`dictionaries`、`documents`、`document_values`（文书填写值，README 未列）。表创建统一在 `scripts/initAdmin.js`（`CREATE TABLE IF NOT EXISTS`）。
- **测试**：jest + supertest（`test-runner.js`）。

### 3.3 `packages/app` — UniApp X 移动端

- **技术栈**：UniApp X、`.uvue` 页面、`.uts` 逻辑（UTS = TS 超集）、HBuilderX 打包、hs-design 组件库。
- **页面结构**（`pages.json`）：
  - 主包：`pages/index`（首页）、`pages/login`、`pages/student`、`pages/mqtt`、`pages/stomp`、`pages/bluetooth`（仅 APP）
  - 分包 `page_im`：IM 登录 / 聊天 / 视频播放 / 图片预览
  - 分包 `page_room`：TRTC 视频通话
- **状态管理**（`store/`）：`user.uts`（登录态/Token）、`student.uts`、`im.uts`（IM 连接/消息/会话）。
- **请求层**：`utils/request.uts` 统一封装（`'/sf-web' + url`，baseUrl 来自 `config.json`：`http://192.168.1.76:3001`）；`interceptors/` 统一拦截器。
- **UTS 原生插件**（`uni_modules/`）：`hs-im`（腾讯 IM）、`hs-trtc`（TRTC 通话）、`hs-trtc-floating`、`hs-bluetooth`、`hs-mqtt`、`hs-stomp`、`hs-design`（组件库）。
- **工具层**（`utils/`）：storage / device / dom / lodash / tool 等。
- ⚠️ 开发注意：所有 `.uts`/`.uvue` 脚本本质是 UTS，须遵守 UTS 语法约束；云打包 vs 本地自定义基座的 `libs/` 切换见 `.agents/skills/uts-plugin/`。

### 3.4 `packages/emr` — 医疗文书编辑器（独立库）

- **定位**：Tiptap / ProseMirror 富文本编辑器封装，作为 workspace 包被 web 引用（`"@cashier/emr": "workspace:*"`）。
- **导出**（`src/index.ts`）：
  - 组件：`EmrEditor`（只读/编辑）、`EmrDesigner`（设计器，web 文书弹窗在用）
  - 插件：`emrEditor(app, { request })` 注册全局组件 + 注入请求方法（`getPluginItem()`/`getAppContext()` 获取）
  - 类型：`EmrElement`、`DocNode`、各节点类型、`TemplateContent`、`DocumentRecord` 等
- **目录**：`components/`（Editor/Designer/Toolbar/PropertyPanel/ComponentPanel/DatePicker/Select/Input 包装）、`extensions/`（VariableExtension、PageBreakExtension）、`hooks/`（useBubble、useEmrApi、useTableContextMenu、useVariableEditing、useComponentPicker）、`types/`、`data/`、`utils/`。
- **构建**：`vite build && vue-tsc --emitDeclarationOnly`，产出 `dist/emr.cjs.js / emr.es.js / index.d.ts / style.css`；发布配置见 `publishConfig`。
- **开发模式**：`pnpm --filter @cashier/emr dev`（vite）；web 通过 workspace:* 直接引用源码。

### 3.5 `packages/mqtt-server` — 本地 MQTT 测试服务器

- **定位**：基于 aedes 的本地 broker，仅供开发联调（无鉴权，全部放行）。
- **入口**：`server.js`，两个监听：
  - MQTT over WebSocket：`ws://localhost:8098/mqtt`
  - MQTT over TCP：`mqtt://localhost:1883`
- **行为**：收到任意 publish 后向固定主题 `uniappx/test/topic-1` 回一条 `收到消息了：<payload>`，用于 app 端 MQTT/STOMP 联调。

### 3.6 `packages/ai-plugin` — VSCode AI 插件（本次重构后）

- **定位**：VSCode 侧边栏 AI 聊天插件，只对接 DeepSeek，无任何 workspace 依赖。
- **构建**：esbuild 打包为单文件 `dist/extension.js`（`node esbuild.config.mjs`，`--watch` 为 dev）；`vsce package` 出 vsix。
- **目录**：
```
src/
├── extension.ts            # 激活入口：组装 ConfigManager/ChatManager/ChatViewProvider，注册视图+命令
├── chat/
│   ├── ChatManager.ts      # 消息记录 + 流式请求编排（streamChat 逐块累积 + 回调）
│   └── ChatViewProvider.ts # 侧边栏 WebviewView 唯一实现（内联 HTML，无构建步骤）
├── config/
│   ├── index.ts            # ConfigManager：读 VS Code 设置 + SecretStorage 存 API Key + 配置流程
│   └── types.ts
├── git/
│   └── GitService.ts       # Git 变更收集（status/diff/新增/删除/重命名），供生成提交总结
├── providers/
│   ├── index.ts            # 注册表（当前仅 deepseek）
│   ├── ILLMProvider.ts     # 提供者接口（chat/streamChat/validateApiKey）
│   ├── DeepSeekProvider.ts # DeepSeek OpenAI 兼容协议实现
│   └── http.ts             # 类型化 JSON 解析辅助
└── types/index.ts          # ChatMessage/ChatOptions/WebviewMessage 等共享类型
```
- **消息协议**（webview ↔ 扩展，`acquireVsCodeApi`）：
  - webview → 扩展：`ready`、`sendMessage{content}`、`stopStreaming`、`clearChat`、`configureProvider`、`gitSummary`
  - 扩展 → webview：`messages`、`streamChunk`、`error`、`configChange`、`gitSummaryRequested`
- **Git 总结功能**：侧边栏 header 📋 按钮（或命令 `ai-plugin.gitSummary`）→ `GitService` 收集工作区未提交变更（`git status --porcelain=v1 -z -uall` 解析状态，`git diff HEAD` 取 diff，未跟踪文件直接读内容，二进制/超大文件跳过）→ `ChatManager.sendGitSummary` 以 system 消息携带 diff 上下文调用 DeepSeek，流式生成"变更概述 / 修改的文件 / 新增的文件 / 删除的文件 / 建议提交信息"。
- **清单**（`package.json` contributes）：activitybar 容器 `ai-plugin-sidebar` → 视图 `ai-plugin-chat`；命令 `openChat/configureProvider/clearChat/gitSummary`（含 `view/title` 菜单 📋⚙🗑）；配置项 `activeProvider/defaultModel/temperature/maxTokens/apiBaseUrl`。
- **调试**：用 VSCode 打开 `packages/ai-plugin`，F5（`.vscode/launch.json` 的 "Run Extension"）。
- ⚠️ 变更注意：`package.json` 的 `viewsContainers.id`（`ai-plugin-sidebar`）与代码中 `ai-plugin-sidebar.focus` 命令是硬关联，改 id 必须同步两处。

---

## 4. 关键约定速查

| 约定 | 说明 |
|---|---|
| API 前缀 | 前端统一 `/sf-web/*`；web 由 vite proxy 转发到 `localhost:3001` 并去掉前缀；app 走 `config.json` 的 baseUrl 直连 |
| 鉴权 | `Authorization: Bearer <JWT>`，登录接口除外；web 端守卫 + store 缓存 userInfo |
| 响应格式 | `{ code: 1|0, data, msg }`，分页多 `total`；前端 `Utils.useRequest` 已封装 |
| 后端分层 | routes → controllers → services → models，外加 validators；新模块五层齐全 |
| 前端分层 | store 收接口、页面收 UI、gridOptions 收表格配置、popup 收弹窗 |
| 弹窗组件 | 统一用 `popup-plugin`（popup.vue + index.ts 模式），见 `popup-component` skill |
| 表格页面 | 统一 `useGridTableMethods` + `gridOptions`，见 `vxe-table-usage` skill |
| 编辑器 | 统一用 @cashier/emr（web 端），勿在 web 内自建编辑器 |
| 移动端 | `.uvue`/`.uts` 必须遵守 UTS 语法，见 `uniapp-mobile`、`uts-plugin` skill |
| 数据库 | 建表/改表放 `backend/scripts/initAdmin.js`（IF NOT EXISTS 保留数据） |

---

## 5. 新增功能标准流程

### 5.1 Web + Backend 新模块（例：新增"公告管理"）

1. **读规范**：`.agents/skills/api-design`、`popup-component`、`store-data-decoupling`、`vxe-table-usage`、`hs-admin-ui`。
2. **后端**：`backend/src/` 新增 `routes/announcements.js` + `controllers/announcementController/`（index+queries+mutations+batchOps）+ `services/AnnouncementService.js` + `models/AnnouncementModel.js` + `validators/AnnouncementValidator.js`；在 `routes/index.js` 注册；建表语句追加到 `scripts/initAdmin.js`。
3. **前端**：`web/src/views/admin/announcement/` 建 `index.vue` + `gridOptions.ts` + `popup/`；`stores/announcement.ts` 收接口；`router/route.ts` 加子路由 + 菜单。
4. **测试**：补 store/gridOptions 单测。

### 5.2 移动端新页面 / 新插件

1. 读 `uniapp-mobile`、`uts-plugin` skill。
2. 页面加入 `pages.json`（或对应分包）；插件按 `uni_modules/<hs-xxx>/` 结构开发（interface.uts + utssdk/各平台实现）。
3. 云打包前删各插件 `utssdk/app-android/libs/`，本地调试前跑 `init-libs.bat`。

### 5.3 VSCode 插件新能力

1. 修改 `src/` 对应层（provider 新增模型能力 / ChatManager 新编排 / ChatViewProvider 新 UI 消息）。
2. `package.json` 的 `commands/menus/views/configuration` 同步更新。
3. `node esbuild.config.mjs` 重新构建 → F5 调试。

---

## 6. 常见坑与提醒

- **根 `pnpm dev` 会连带启动所有包**（含 ai-plugin 的 watch 和 emr 的 vite dev），只想联调 web+backend 时用 `pnpm dev:web` / `pnpm dev:backend`。
- **hs-admin-ui 是 OSS tgz 私有包**：版本不可从 npm 更新，升级需替换 `web/package.json` 中的 tgz URL（注意 lockfile 同步）。
- **emr 是 workspace 包**：改动 emr 源码，web 热更新即可生效；但要发布/打包需先 `pnpm --filter @cashier/emr build`。
- **backend 控制器异常**：务必经 `wrap()`，否则错误不会转成统一 `code:0` 格式。
- **批量删除用 POST**（DELETE 请求体在部分客户端被丢弃），后端路由名 `batch/delete`。
- **ai-plugin**：webview 必须用 `acquireVsCodeApi()` 通信；侧边栏容器 id 与 focus 命令硬关联。
- **app 直连后端 IP**：`config.json` 的 `baseUrl` 是开发机局域网 IP，换环境需改。

---

## 7. 开发规范（.agents/skills）

| Skill | 适用场景 |
|---|---|
| `api-design` | 创建/修改接口路由、控制器 |
| `popup-component` | 新增/编辑/创建/详情弹窗 |
| `store-data-decoupling` | 创建/重构 Pinia Store（数据与页面解耦） |
| `vxe-table-usage` | 表格/列表页面（useGridTableMethods） |
| `hs-admin-ui` | Element Plus / VXE-Table / 工具函数使用 |
| `uniapp-mobile` | UniApp X 移动端页面、生命周期、状态管理 |
| `uts-plugin` | UTS 插件开发、编译/运行错误、云打包 vs 本地基座 |

> 总纲见 `.agents/PROJECT_RULES.md`：动手前先读对应 skill，按其中目录结构、命名、代码模式实现。

---

## 8. ai-plugin 功能规划（待实现：上下文加载 + 代码修改）

> 现状：纯文本对话、流式输出、Git 变更总结（system 上下文注入）已实现；
> 编辑器上下文加载与 AI 修改代码尚未实现。以下为规划，按依赖顺序排列。

### 8.1 现状能力盘点

| 能力 | 现状 | 说明 |
|---|---|---|
| 纯文本对话 | ✅ | `ChatManager.sendMessage` 只发文本 |
| 流式输出 | ✅ | `streamChunk` 协议 |
| Git 变更总结 | ✅ | `GitService` 收集 → system 消息注入（**已有上下文注入雏形**） |
| 编辑器上下文 | ❌ | 拿不到当前文件 / 选区内容 |
| 文件读取 | ❌ | 无法按路径读工作区文件给 AI |
| AI 修改代码 | ❌ | 只能输出文本，不能落盘 |
| 应用 / 预览 / 回滚 | ❌ | 无 diff 预览、无撤销 |

### 8.2 阶段一：上下文加载（让 AI "看得见"代码）

1. **当前文件 / 选区上下文**
   - `vscode.window.activeTextEditor` 拿当前文件路径、语言、全文或选中部分
   - 新增 `src/context/ContextService.ts`（仿照 `GitService` 结构）：读文件、列工作区文件、token 估算截断
   - 消息协议新增 `attachFile` / `attachSelection`（webview → 扩展）
2. **对话中 @ 引用文件（Copilot 式）**
   - 输入 `@` 弹出文件选择（`vscode.workspace.findFiles` 检索），选中后作为"附件"标签显示
   - 发送时以 system 消息注入文件内容（复用 `sendGitSummary` 的 system 注入模式）
3. **上下文容量管理**
   - token 估算（中文约 1 token/字 + 英文按 4 字符/token），超限截断
   - system 上下文**不入对话历史**，避免污染后续轮次

### 8.3 阶段二：AI 修改代码（让 AI "动手"）

1. **结构化修改输出协议**：模型按约定输出 `【修改文件】path` + diff 或完整内容（`【新建文件】`/`【删除文件】` 同理）
2. **新增 `src/edit/EditService.ts`**：解析模型输出 → 提取文件路径 + 新内容
3. **修改预览（安全关键）**：不直接落盘，先 `vscode.diff` 对比视图或 webview 内 diff 让用户确认
4. **应用 + 回滚**：`vscode.workspace.fs.writeFile` 写文件；写前备份到 `context.globalStorageUri`，提供回滚命令；只能改工作区内文件

### 8.4 阶段三：UI 交互

- header 按钮（📄 当前文件 / 选中区）、`@` 文件选择器
- 消息气泡上方附件标签 `📎 src/a.ts`（可移除）
- 修改确认：webview 内 diff + "应用/取消"，或调 VSCode diff 视图
- 修改历史：备份目录 + 回滚按钮
- 命令面板：`ai-plugin.addFileContext`、`ai-plugin.applyEdit` 等

### 8.5 架构影响（对现有代码的改动量）

| 文件 | 改动 | 规模 |
|---|---|---|
| `src/context/ContextService.ts` | 新增 | 中 |
| `src/edit/EditService.ts` | 新增 | 中 |
| `src/chat/ChatManager.ts` | 增加 `sendMessageWithContext(content, attachments)` | 小（复用 system 注入） |
| `src/chat/ChatViewProvider.ts` | 新增消息协议 + 附件 UI | 中 |
| `src/chat/webview.html` | 附件标签 / diff 确认 UI | 中 |
| `package.json` | 新增命令 + 菜单 | 小 |
| `src/types/index.ts` | 新增协议类型 | 小 |

> 现有 system 消息注入模式（`sendGitSummary`）可直接复用，无需改 provider 层，侵入性可控。
